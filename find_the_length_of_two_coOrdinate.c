#include<stdio.h>
struct calculate{
float crd;
float length;
}c, x1,x2,y1,y2, temp;
int main(){

scanf("%f",&x1.crd);
scanf("%f",&x2.crd);
scanf("%f",&y1.crd);
scanf("%f",&y2.crd);
temp.length = sqrt((x2.crd-x1.crd)*(x2.crd-x1.crd)+(y2.crd-y1.crd)*(y2.crd-y1.crd));
printf("%f",temp.length);

return 0;
}
